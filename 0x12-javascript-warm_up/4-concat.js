#!/usr/bin/node
// script that prints two argument passed to  it, in the following format: "its"
const args = process.argv.slice(2);
if (!argv[0]) {
	console.log(args[0] + ' is ' + args[1]);
} else if (args.length === 1) {
	console.log(args[0] + ' is ' + args[1]);
} else {
	console.log(args[0] + ' is ' + args[1]);
}
